If you want a file to write notes in without having to commit the changes to github,
then add this file to the .gitignore file. For example, I have added 
'src/local/tempNotes.txt' to .gitignore, so if you create that file you can change it without
it showing up as a non-committed change.